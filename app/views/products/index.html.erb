<div class="container">
  <i class="fa fa-cutlery" aria-hidden="true"></i>
  <h1>Yes, we're a cheese shoppe.</h1>
</div>
<hr>
<div class="container">
  <h2>Here's what we have in stock today</h2>
  <br>
</div>
<div class="container">
  <div class="row">
      <% @products.each do |product| %>
      <!-- The below class goes inside the loop if I want this to be flexbox-like
      Otherwise it will render everything on top of each other -->
        <div class="col-md-6">
        <h2><a href="/products/<%= product.id %>"><%= product.name %></a></h2>
        <p>Only $<%= product.price %>!</p>
        <div class="well"><img src= <%= product.images.first.url %> class="img-circle img-responsive" alt="Cheese example">
        </div>
        <p><%= product.description %></p>
        </div>
      <%end%>
    <div class="row">
      <div class="col-md-12">
        <hr>
        <h3><a href="/products/new">Add new...</a></h3>
      </div>
    </div>
</div>